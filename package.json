{
    "name": "gvas-json-converter",
    "description": "Convert Unreal Engine .sav files to JSON and vice versa.",
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "test": "vitest run"
    },
    "dependencies": {},
    "devDependencies": {
        "benchmark": "^2.1.4",
        "vite": "^8.0.10",
        "vitest": "^4.1.5"
    }
}
